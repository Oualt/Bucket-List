{% extends 'base.html.twig' %}

{% block title %} 
Liste d'idées | {{ parent() }}
{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Liste d'idées ✅</h1>

    {# This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'D:/wamp64/www/ENI/Bucket-List/src/Controller/WishController.php'|file_link(0) }}">src/Controller/WishController.php</a></code></li>
    </ul> #}
    <section>
    {% if wishes is empty %}
        <p>No wishes found, please add one!</p>
    {% else %}
        {% for wish in wishes %}
        <article>
        💡 
        <a href="{{ path('wish_details', {"id" : wish.id})}}" title=wishTitle>
         {{wish.title}}  [{{wish.category.name}}]
        </a>
        </article>
    {% endfor %}
    {% endif %}
    </section>
</div>
{% endblock %}
